/node_modules
/.build
/.husky

# dotenv
.env*
!.env.example

# AI - .claude folder is public (open source)
# Only ignore build artifacts and dependencies inside it
.claude/mcp/*/node_modules
.claude/mcp/*/dist